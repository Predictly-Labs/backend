# Server
PORT=3001
NODE_ENV=development

# Rate Limiting
# Set to 'true' to disable rate limiting (useful for development/testing)
# Set to 'false' or remove in production
DISABLE_RATE_LIMIT=true

# Database (PostgreSQL)
# Used for: user data, markets, votes, nonce storage, rate limiting
DATABASE_URL="postgresql://username:password@localhost:5432/predictly?schema=public"

# Pinata (IPFS)
PINATA_API_KEY=your_pinata_api_key
PINATA_SECRET_KEY=your_pinata_secret_key
PINATA_GATEWAY_URL=https://gateway.pinata.cloud/ipfs

# Privy Auth (DEPRECATED - kept for backward compatibility)
PRIVY_APP_ID=your_privy_app_id
PRIVY_APP_SECRET=your_privy_app_secret

# Movement Network (Testnet)
MOVEMENT_RPC_URL=https://testnet.movementnetwork.xyz/v1
MOVEMENT_PRIVATE_KEY=your_private_key_here
MOVEMENT_CONTRACT_ADDRESS=0x9161980be9b78e96ddae98ceb289f6f4cda5e4af70667667ff9af8438a94e565

# Relay Wallet Configuration
# The relay wallet pays gas fees for market initialization on-chain
# Use the same private key as MOVEMENT_PRIVATE_KEY (the wallet that deployed the contract)
RELAY_WALLET_PRIVATE_KEY=your_private_key_here
RELAY_WALLET_MIN_BALANCE=10  # Minimum balance in MOVE tokens (warning threshold)

# JWT Secret (for backend sessions)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Admin Token (for protecting admin endpoints)
# Generate a secure random string for production
# Example: openssl rand -hex 32
ADMIN_TOKEN=your-secret-admin-token-change-this-in-production

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
